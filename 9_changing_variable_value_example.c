// Create an integer variable number with value 31. Print the variable. Change the value of the variable to 99.


#include <stdio.h>

int main() {
  
  int number=31;
  
  printf("%d", number);
  
  number=99;
  printf(" %d", number);

    return 0;
}